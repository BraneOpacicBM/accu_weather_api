import React from 'react';
import './Spinner.scss';

const Spinner = (props) => {
    return (
        <div className="SpinnerHolder">
            <div className="Spinner"></div>
        </div>
    )
}

export default Spinner;